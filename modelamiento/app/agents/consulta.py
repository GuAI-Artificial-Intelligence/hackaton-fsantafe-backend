import config

class ConsultaAgent(object):
    def __init__(self):
        self.max_medicos = 2
        self.medicos_disponibles = 2

        def cambiar_fase_paciente(self, paciente):
            if self.available_staff > 0:
                nueva_fase = config.FASES['consulta']
            return nueva_fase

    